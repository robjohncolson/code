# Railway Server Environment Variables
# CRITICAL: These are SERVER-ONLY secrets
# NEVER expose these in client-side code

# ============================================
# SUPABASE CREDENTIALS (Server-Only)
# ============================================

# Supabase project URL
SUPABASE_URL=https://xxxxxxxxxxxxx.supabase.co

# Public anon key (read-only operations)
SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6InhYeHh4eHh4eHh4eHh4eHh4Iiwicm9sZSI6ImFub24iLCJpYXQiOjE2OTc1Nzg5ODYsImV4cCI6MjAxMzE1NDk4Nn0.xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# ⚠️ SERVICE KEY - NEVER EXPOSE TO CLIENT
# This key has full database access - server-only
SUPABASE_SERVICE_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6InhYeHh4eHh4eHh4eHh4eHh4Iiwicm9sZSI6InNlcnZpY2Vfcm9sZSIsImlhdCI6MTY5NzU3ODk4NiwiZXhwIjoyMDEzMTU0OTg2fQ.xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# ============================================
# SERVER CONFIGURATION
# ============================================

# Server port (Railway sets this automatically)
PORT=3000

# Node environment (development, production)
NODE_ENV=production

# ============================================
# CORS & SECURITY
# ============================================

# Allowed origins (comma-separated)
# In production, set to your actual domain
ALLOWED_ORIGINS=https://yourdomain.com,https://your-app.netlify.app

# In development, allow localhost
# ALLOWED_ORIGINS=http://localhost:8000,http://127.0.0.1:8000

# ============================================
# CACHE CONFIGURATION (Optional)
# ============================================

# Cache TTL in seconds (default: 30)
CACHE_TTL=30

# Maximum cache entries (default: 1000)
MAX_CACHE_SIZE=1000

# ============================================
# HOW TO SET THESE IN RAILWAY
# ============================================
# 1. Go to Railway dashboard > Your Project > Variables
# 2. Click "New Variable"
# 3. Add each variable above (except PORT - Railway sets that)
# 4. Click "Deploy" to apply changes
#
# Security note:
# - Railway encrypts environment variables
# - They are never exposed in logs or client code
# - Use Railway's secret scanner to detect leaks

# ============================================
# SECURITY CHECKLIST
# ============================================
# ✓ .env file in .gitignore
# ✓ SERVICE_KEY never in client code
# ✓ CORS origins set to production domains only
# ✓ Environment variables set in Railway dashboard
# ✗ DO NOT commit this file with real values